import React from 'react';
import Navigation from './navigation';

const Header = () => {
    return (
        <header>
            <div id="logo">
                <h1>Bet Net</h1>
            </div>
            <Navigation/>
        </header>
    )
};

export default Header;
